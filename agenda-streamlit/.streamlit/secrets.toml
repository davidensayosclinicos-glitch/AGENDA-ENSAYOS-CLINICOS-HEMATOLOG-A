# Configuracion LOCAL TEMPORAL (no usar en produccion)
# Permite usar agenda_ensayos.db sin PostgreSQL.
ALLOW_SQLITE_FALLBACK = "1"

# Para produccion (Streamlit Cloud), usar Secrets de la app:
# DATABASE_URL = "postgresql://usuario:password@host:5432/dbname?sslmode=require"
# ALLOW_SQLITE_FALLBACK = "0"

# Claves para el analizador de protocolos IA (Groq/OpenAI/OpenRouter)
groq_api_key = ""
openai_api_key = ""
openrouter_api_key = ""
